import React from "react";
import Router from "./Router";
import ClientsContainer from "./ClientsContainer";

const App = () => {
    return (
        <div>
        <ClientsContainer />
        < Router />
        </div>
    );
};

export default App;